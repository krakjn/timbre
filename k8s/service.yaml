apiVersion: v1
kind: Service
metadata:
  name: timbre
  labels:
    app: timbre
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30000
    protocol: TCP
    name: http
  selector:
    app: timbre 